using UnityEngine;

public class ejericio3 : MonoBehaviour
{
    int[] vidas = new int[3] { 100, 200, 300};
   string[] posiones = new string[3] {"veneno", " magia", "furia"};
    void Start()
    {
        print("vidas1: " + posiones[0]);
        print("vidas2: " + posiones[1]);
        print("vidas3: " + posiones[2]);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

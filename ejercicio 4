using System.Collections.Generic;
using UnityEngine;

public class ejericio4 : MonoBehaviour
{
    List<string> enemigos = new List<string>(); 
    void Start()
    {
        enemigos.Add("antimage");
        enemigos.Add("Axe");
        enemigos.Add("invoker");
      print("El numero de enemigos es:" + enemigos.Count);

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

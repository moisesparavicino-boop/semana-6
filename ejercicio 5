using System.Collections.Generic;
using UnityEngine;

public class ejercicio5 : MonoBehaviour
{
       int[] vidas = new int[3];
      List<string> Inventario = new List<string>();

     {
        vidas[0] = 80;
        vidas[1] = 60;
        vidas[2] = 40;

        Inventario.Add("casco");
        Inventario.Add("bate");

       spring("Su vida es:" +  vidas[0]);
       spring("Cantidad de inventario:" + Inventario.Count);
       spring("Su item es:" + Inventario[0]);


    // Update is called once per frame
    void Update()
    {
        
    }
}

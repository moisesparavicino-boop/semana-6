using UnityEngine;

public class ejercicio6 : MonoBehaviour
{
    int[] exp = new int[5];

    {
        exp[0] = 30;
        exp[1] = 50;
        exp[2] = 70;
        exp[3] = 90;
        exp[4] = 100;

       print("Su exp:" + exp[0]);
        print("Su exp:" + exp[exp.Length - 1]);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
